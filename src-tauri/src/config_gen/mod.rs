//! Generates `httpd.conf`, `nginx.conf`, `php.ini`, `my.cnf` fragments from
//! the SQLite source-of-truth state. Never edit bundled config files in place;
//! always regenerate. Implementation begins in Phase 1.
